# SpaceK TeamChat — 무료로 쓰는 방법

`SpaceK_TeamChat.html` 파일 하나로 동작합니다. **서버를 켤 필요가 없습니다.**
전부 무료로 구성했습니다.

| 기능 | 무료 수단 | 비고 |
|------|-----------|------|
| 팀 실시간 채팅·저장·파일공유 | **Supabase 무료 티어** | 카드 불필요 |
| @AI · 회의 자동요약 · 브리핑 | **Google Gemini 무료 API** | 카드 불필요, 15회/분·1,500회/일 |
| 회의 음성 → 텍스트 | **브라우저 내장 음성인식** | Chrome 권장, 무료 |

---

## A. 지금 바로 (설정 0분) — 혼자 체험

1. `SpaceK_TeamChat.html`을 더블클릭해 브라우저로 엽니다.
2. 첫 화면에서 Supabase 칸은 **비워두고**, 아래 두 개만:
   - Gemini 키(선택): 있으면 AI 기능이 켜집니다 (발급법은 C).
   - 공용 비번: 기본 `1234`.
3. "저장하고 시작" → 이름 + 비번으로 입장.

이 모드는 **이 기기에서만** 저장됩니다(로컬 데모). 채팅·회의녹음·분석·브리핑을 혼자 다 써볼 수 있습니다. 팀원과 공유하려면 B로 진행하세요.

---

## B. 팀원과 실시간 공유 (무료 Supabase, 약 10분)

### 1) 무료 프로젝트 만들기
1. https://supabase.com 접속 → **Start your project** (GitHub/구글 로그인, 무료).
2. **New project** 생성 (이름·비번 지정, 리전은 `Northeast Asia (Seoul)` 권장).
3. 좌측 **Project Settings → API** 에서 두 값 복사:
   - **Project URL** (`https://xxxx.supabase.co`)
   - **anon public key**

### 2) 데이터베이스 설정 (SQL 한 번 실행)
좌측 **SQL Editor → New query** 에 아래를 붙여넣고 **Run**:

```sql
-- 직원 계정 (이름 + 비번)
create table if not exists members (
  name text primary key,
  pass text not null
);

-- 채팅 메시지
create table if not exists messages (
  id bigint generated always as identity primary key,
  name text not null,
  text text,
  file_url text,
  file_type text,
  file_name text,
  created_at timestamptz default now()
);

-- 회의 기록
create table if not exists meetings (
  id bigint generated always as identity primary key,
  title text,
  transcript text,
  summary jsonb,
  created_at timestamptz default now()
);

-- 로그인 검증 함수 (비번을 앱으로 내려보내지 않고 서버에서 확인)
create or replace function check_login(p_name text, p_pass text)
returns boolean language sql security definer as $$
  select exists(select 1 from members where name = p_name and pass = p_pass);
$$;

-- 실시간 반영 + 접근 정책
alter table messages enable row level security;
alter table meetings enable row level security;
create policy "read msg"  on messages for select using (true);
create policy "write msg" on messages for insert with check (true);
create policy "delete msg" on messages for delete using (true);
create policy "read meet"  on meetings for select using (true);
create policy "write meet" on meetings for insert with check (true);
alter publication supabase_realtime add table messages;

-- 직원 등록 (원하는 만큼 추가, 비번은 각자 지정)
insert into members(name, pass) values
  ('홍길동','1234'),
  ('김영희','1234')
on conflict (name) do nothing;
```

> 직원을 추가하려면 마지막 `insert` 줄을 바꿔 다시 Run 하면 됩니다. 비번은 여기서 서버에만 저장되어 **앱 화면·코드에 노출되지 않습니다.**

### 3) 파일 공유용 저장소 만들기
좌측 **Storage → New bucket** → 이름 `files` → **Public bucket** 체크 → 생성.

### 4) 앱에 연결
1. `SpaceK_TeamChat.html` 첫 설정 화면(또는 앱 안 **설정** 탭)에 위에서 복사한
   **Project URL**, **anon key**, **Gemini 키**를 입력 → 저장.
2. 배지가 **"팀 연결"**(초록)로 바뀌면 완료.
3. **같은 HTML 파일을 팀원에게 전달**하고, 팀원은 자기 이름 + 비번으로 입장하면
   채팅·회의기록이 실시간으로 공유됩니다.

> 파일을 각자 열어도 같은 Supabase에 연결되므로 서로 실시간으로 보입니다.
> (배포까지 원하면 이 HTML을 Netlify/Vercel 무료 호스팅에 올려 URL로 공유해도 됩니다.)

---

## C. 무료 Gemini API 키 발급 (AI 기능용)

1. https://aistudio.google.com/apikey 접속 (구글 로그인).
2. **Create API key** → 생성된 키 복사 → 앱 설정에 붙여넣기.
3. 카드 등록 불필요. 무료 한도: 분당 15회 / 하루 1,500회 (소규모 팀 충분).

---

## 한계 (알아두기)

- **음성인식**은 브라우저 기반(무료)이라 화자 구분이 없고 긴 회의·소음에 정확도 한계가 있습니다. 필요하면 나중에 유료 전사 API로 업그레이드할 수 있습니다.
- 로컬 데모 모드는 기기 간 공유가 안 됩니다(팀 공유는 B 필요).
- anon key는 공개용 키라 민감정보를 다루는 외부 서비스로 키우려면 개인 계정·강력한 RLS로 강화하세요. 사내 팀 내부용으로는 충분합니다.

---

## 문제 해결

- **입장이 안 됨(팀 모드)** → SQL의 `check_login` 함수와 `members`에 내 이름·비번이 있는지 확인.
- **AI가 안 됨** → 설정에 Gemini 키가 있는지, 하루 한도(1,500회)를 넘지 않았는지 확인.
- **음성인식 안내가 뜸** → Chrome에서 열고 마이크 권한을 허용하세요. 안 되면 회의록을 붙여넣고 "AI로 분석" 하면 됩니다.
